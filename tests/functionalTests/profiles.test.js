/* eslint-disable no-undef */
'use strict';

const client = require('../apiSupertest');

describe('Dummy test', () => {
    test('Should pass', () => {
        expect('result').toStrictEqual('result')
    });
})